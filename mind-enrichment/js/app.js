// ============================================================================
// APP.JS — entry point. Runs once all other scripts have loaded.
// ============================================================================

/* ---------------- INIT ---------------- */

renderFilterPills();

renderResults(EDUCATORS);

initFilterPickers();

loadPersistedTheme();

restoreSession();

